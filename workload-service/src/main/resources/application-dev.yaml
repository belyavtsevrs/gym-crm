server:
  port: 8083

spring:
  application:
    name: workload-service
  datasource:
    url: jdbc:h2:mem:workloadDb
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  h2:
    console:
      enabled: true
      path: /h2-console
  data:
    mongodb:
      uri: mongodb+srv://belyavtsevrs_db_user:ptIrm3BYWpB26URF@cluster0.nbie75n.mongodb.net/workloadDb?appName=Cluster0

management:
  health:
    refresh:
      enabled: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html