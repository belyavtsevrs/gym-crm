server:
  port: 8083

spring:
  application:
    name: workload-service
management:
  health:
    refresh:
      enabled: true
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html